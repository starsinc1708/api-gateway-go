api-gateway:
  port: 8082
  endpoint: tg-updates

bot-modules:
  anon-chat:
    grpc:
      host: localhost
      port: 5002
    allowed-updates:
      private_chat:
        - message
        - callback_query
        - my_chat_member
        - chat_member
        - message_reaction
        - message_reaction_count